       var current = 0;        var handler;        $(function(){            set_img(this.index);            var bts =  $("#img_bt li");            for (i=0; i<bts.length; i++){                bts[i].index = i;                $(bts[i]).hover(function(){                    set_img(this.index);                });            }            handler = setInterval(auto_img, 2000);            var images = $("#news img");            $(images).each(function(){                $(this).hover(function(){clearInterval(handler)},function(){handler = setInterval(auto_img, 3000);});            });        });        function set_img(whtich){            var bts =  $("#img_bt li");            if (whtich <bts.length){                for (i=0; i<bts.length; i++){                    var images = $("#news img");                    for (i=0; i<images.length; i++){                        $(images[i]).css("display", "none");                    }                    $(images[whtich]).css("display", "block");                    current = whtich;                }            }        }        function auto_img(){		      var bts =  $("#img_bt li");			  var i=bts.length;            current = (++current)%i;            set_img(current);        }    